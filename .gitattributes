# Auto detect text files and perform LF normalization
* text=auto

# Binary assets — don't touch line endings
*.woff2 binary
*.woff binary
# Vendored library — exclude from language stats and diffs
Spill_de_Tea/vendor/* linguist-vendored
